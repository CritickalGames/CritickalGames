name: My JavaScript Action
description: A custom JavaScript action for my repository

inputs:
  my-input:
    description: 'An example input'
    required: true
    default: 'default value'

runs:
  using: 'node12'
  main: 'path/to/entrypoint.js'
