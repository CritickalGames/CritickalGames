import json
import os
from helpers import types, set_config

FILEPATH = "../"
PATH = "ReadmeGenerator/"
FILENAME_BASE = f"{PATH}config_base.json"
FILENAME_PROJECTS = f"{PATH}config_projects.json"

#leer ambos archivos; conseguir los elementos del archivo; convertir en JSON
#Estudiar el HELPER.PY y SCRAPER.py; LIMPIAR